pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                echo 'build is in progress...'
            }
        }
        stage('test') {
            steps {
                echo 'test is in progress...'
            }
        }
        stage('deploy') {
            steps {
                echo 'deploy is in progress...'
            }
        }
    }
}